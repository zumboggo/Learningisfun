# Production build values come from GitHub Actions secrets, not this file —
# anything written here is committed to a public repo and inlined into the
# JavaScript that ships to every browser.
#
# VITE_TEACHER_CODE is set as a repository secret and injected by
# .github/workflows/deploy.yml. To change it:
#   gh secret set VITE_TEACHER_CODE
