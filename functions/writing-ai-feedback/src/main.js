import { Client, Databases, ID, Query } from 'node-appwrite';
export default async ({ req, res, error }) => { try { const userId=req.headers['x-appwrite-user-id']; if(!userId)return res.json({error:'Authentication required'},401); const {submissionId}=JSON.parse(req.bodyText||'{}'); const client=new Client().setEndpoint(process.env.APPWRITE_ENDPOINT).setProject(process.env.APPWRITE_FUNCTION_PROJECT_ID).setKey(process.env.APPWRITE_API_KEY); const db=new Databases(client),databaseId=process.env.APPWRITE_DATABASE_ID||'main'; const submission=await db.getDocument(databaseId,'writing_submissions',submissionId),prompt=await db.getDocument(databaseId,'writing_prompts',submission.promptId); if(!prompt.aiFeedbackEnabled)return res.json({error:'AI feedback is disabled'},403); const memberships=await db.listDocuments(databaseId,'class_members',[Query.equal('classId',submission.classId),Query.equal('userId',userId),Query.limit(1)]); if(!memberships.total&&prompt.teacherId!==userId)return res.json({error:'Not enrolled'},403); if(submission.authorId!==userId&&prompt.teacherId!==userId)return res.json({error:'Not your submission'},403); const studentText=submission.submittedMarkdown||submission.draftMarkdown;if(!studentText?.trim())return res.json({error:'There is nothing written yet'},400); const response=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENROUTER_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENROUTER_MODEL||'openai/gpt-4o-mini',temperature:.4,response_format:{type:'json_object'},messages:[{role:'system',content:'You are a constructive writing coach. Return JSON with a concise www string and an improvements array of exactly three specific actionable suggestions. Do not grade.'},{role:'user',content:`Assignment: ${prompt.title}\nPrompt: ${prompt.promptMarkdown}\nRubric: ${prompt.rubricJson}\n\nStudent writing:\n${studentText}`}]})}); if(!response.ok)throw new Error(`AI provider returned ${response.status}`);const body=await response.json(),result=JSON.parse(body.choices?.[0]?.message?.content||'{}'),now=new Date().toISOString(),payload={submissionId,wwwSummary:result.www||'',improvementsJson:JSON.stringify(result.improvements||[]),model:process.env.OPENROUTER_MODEL||'openai/gpt-4o-mini',generatedAt:now};const existing=await db.listDocuments(databaseId,'writing_ai_feedback',[Query.equal('submissionId',submissionId),Query.limit(1)]);if(existing.total)await db.updateDocument(databaseId,'writing_ai_feedback',existing.documents[0].$id,payload);else await db.createDocument(databaseId,'writing_ai_feedback',ID.unique(),payload);return res.json({www:payload.wwwSummary,improvements:result.improvements||[],generatedAt:now}); } catch(err){error(err.message);return res.json({error:'Could not generate feedback'},500);} };
