import { TextPurposeControls, type TextPurpose } from './TextPurpose';
import { useId, useState } from 'react';
import { textReleaseAt } from '@/services/text-schedule';
export function ClassReadingDate({name,value,onChange,disabled=false,purpose={},onPurposeChange}:{name:string;value:string;onChange:(value:string)=>void;disabled?:boolean;purpose?:TextPurpose;onPurposeChange?:(value:TextPurpose)=>void}) {
  const [open,setOpen]=useState(false),id=useId();
  return <div><div className="flex flex-wrap items-center gap-2"><button type="button" disabled={disabled} aria-label={`Set reading due date for ${name}`} aria-expanded={open} aria-controls={id} onClick={()=>setOpen(!open)} className="flex items-center gap-2 rounded-lg border px-2 py-1 text-sm disabled:opacity-40"><svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7"><rect x="3" y="5" width="18" height="16" rx="3"/><path d="M7 3v4m10-4v4M3 11h18"/></svg>{value||'No due date'}</button>{onPurposeChange&&<TextPurposeControls value={purpose} onChange={onPurposeChange} disabled={disabled}/>}</div>{open&&<div id={id} className="mt-2 space-y-2 text-xs"><input aria-label={`Reading due date for ${name}`} type="date" value={value} disabled={disabled} onChange={e=>onChange(e.target.value)} className="rounded border p-2"/>{value&&<button type="button" onClick={()=>onChange('')} className="ml-2 underline">Clear</button>}<p className="text-gray-500">{value?`Available ${new Date(textReleaseAt(value)).toLocaleString('en-US',{timeZone:'Asia/Shanghai',month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})} (China time).`:'Available immediately, in the week posted.'}</p></div>}</div>;
}
