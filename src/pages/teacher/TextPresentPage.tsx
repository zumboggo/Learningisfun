// Presentation uses the exact same reader as students, without the app navigation.
// Keep this route alias so existing Present links and bookmarks still work.
export { TextReaderPage as TextPresentPage } from '@/pages/TextReaderPage';
